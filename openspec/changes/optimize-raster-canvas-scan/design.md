## Context

動機與驗收條件見 [proposal.md](proposal.md)。本文件只處理「怎麼做」。

### 現行每層流程

光柵化由 [SLAPrintSteps.cpp:1686-1715](../../../third_party/prusaslicer_fork/src/libslic3r/SLAPrintSteps.cpp#L1686-L1715) 的 `lvlfn` 與 [SLAArchiveWriter.hpp:97-109](../../../third_party/prusaslicer_fork/src/libslic3r/Format/SLAArchiveWriter.hpp#L97-L109) 的 `draw_layers()` 組成。每一層在一條 TBB 執行緒上依序執行：

```
ThreadBoundRasters::acquire()
  └─ reset()                    ① 全幅面寫入（clear 整張緩衝）
lvlfn
  ├─ draw(model polygons)          AGG 掃描線，只寫入被覆蓋的像素
  ├─ apply_postprocess()        ② 全幅面讀取（SL1 AA 量化，8 位元組全 0/全 FF 跳過）
  └─ draw_binary(support)          AGG 掃描線（門檻 gamma）
encode(RLERasterEncoder)        ③ 全幅面逐位元組讀取
encode(PNGPreviewEncoder)       ④ 全幅面讀取（區塊平均，逐像素累加）
```

①～④ 的成本只與幅面大小有關，與畫了多少東西無關。16K 幅面每層 94,197,600 位元組，而點亮比例通常低於一成。

### 既有約束（必須維持）

- **raster 與執行緒綁定**：`ThreadBoundRasters` 以 `tbb::enumerable_thread_specific` 保證一個 raster 只被一條執行緒使用（spec `sla-raster-performance`）。本設計新增的狀態全部掛在 raster 實例上，因此自動繼承這個保證。
- **`reset()` 為純虛擬**：忘了清除累積狀態會變成編譯錯誤，而不是靜默的錯誤輸出（[RasterBase.hpp:119-129](../../../third_party/prusaslicer_fork/src/libslic3r/SLA/RasterBase.hpp#L119-L129)）。本設計沿用同一個原則：寧可編譯失敗，也不要靜默出錯。
- **`reset()` 現行註解**明確寫著：只清除上一層寫過的區域，前提是所有消費端對「哪個區域」有一致認知（[AGGRaster.hpp:218-223](../../../third_party/prusaslicer_fork/src/libslic3r/SLA/AGGRaster.hpp#L218-L223)）。本設計的格子表就是這個唯一的共同認知。
- **RLE 格式須與 `agent/prz_encoder.py:_rle_encode_layer` 逐位元組一致**，後端直接拿來組 PRZ。
- **預覽縮放分母只會是 4、5、8、10**（[preview_scale.py:33](../../../agent/preview_scale.py#L33)），且全部走整數倍快路徑。
- **fork 以 C++17 建置**（`CMAKE_CXX_STANDARD 17`），不能使用 `std::countr_zero`。
- **其他使用者**：`AnycubicSLA` 的編碼器、GUI 字型預覽工作、`RasterToPolygons`（`read_pixel`）、`test_marchingsquares` 都直接讀取完整緩衝。它們不在本次優化範圍內，但不得被改壞。

## Goals / Non-Goals

**Goals:**

- 讓 ①～④ 的成本隨「實際寫入的面積」變化，而不是隨幅面大小變化。
- 所有改動維持逐位元組相同的層檔與預覽圖，並能在同一支 binary 內切回原演算法做對照。
- 為「漏記寫入區域」這類只在特定排程下出現的錯誤，提供結構性的防護與可重複的偵測手段。
- 重建一套不含病患資料、可在不同機器重現的基準與比對工具。

**Non-Goals:**

- 不加速 blur 路徑。`blur > 0` 時回到全幅面後處理（見 D5）。
- 不使用 SSE2／AVX2／NEON 等平台專屬指令集；第 1 步只用可攜的 64 位元字組運算（SWAR）。
- 不改 `RasterEncoder`、`RasterPostProcessor` 既有簽章的語意，不影響 `AnycubicSLA` 與 GUI 使用端。
- 不改 agent 的 CLI 參數、API 與 Python 程式。
- 不保證 Windows 與 macOS 之間輸出位元一致。

## Decisions

### D1：正確性論證的核心——RLE 輸出只由「最大同色段分解」決定

`RLERasterEncoder` 沒有單段長度上限（`run` 為 `uint32_t`，只在值改變時 `emit_run`），因此輸出位元組完全由「把整張緩衝以列優先順序切成最大同色段」的結果決定：同一組段、同一個順序，就產生同一串位元組與同一個檢查碼。

所以任何加速方法，只要能證明它**產生相同的最大同色段序列**，就一定逐位元組相同——不論它一次跳 1 個、8 個還是 80 個像素。第 1 步與第 2 步的 RLE 改動都以這條論證為依據，單元測試則以原演算法當對照組做差分比對（見 D10）。

段長的格式上限是 2^28 − 1（4 個位元加 3 個延伸位元組，[RasterBase.cpp:184-187](../../../third_party/prusaslicer_fork/src/libslic3r/SLA/RasterBase.cpp#L184-L187)）。機隊最大幅面為 94,197,600 像素，全黑層是單一段，仍在範圍內。超過 2^28 像素的幅面在原實作就會輸出錯誤，本設計不改變這個既有行為，只在文件與測試中註明前提。

### D2：第 1 步 RLE——64 位元字組比對加尾端零位計數

只在 `num_components == 1`（灰階，正式流程恆成立）時啟用；否則走原迴圈。

```
cur = src[0]; run = 0; p = 0
while p + 8 <= n:
    w   = load_u64(src + p)            // memcpy，不做指標轉型
    x   = w XOR (cur × 0x0101010101010101)
    if x == 0:                         // 8 個位元組都等於 cur
        run += 8; p += 8; continue
    k = ctz64(x) >> 3                  // 前 k 個位元組仍等於 cur（小端序）
    run += k; p += k
    emit(cur, run); cur = src[p]; run = 1; p += 1
while p < n:                           // 尾端不足 8 位元組，逐位元組
    ...原邏輯...
emit(cur, run)
```

- **讀取**：以 `memcpy` 載入 `uint64_t`。MSVC 與 Clang 都會把它編成單一指令，同時避開未對齊存取與 strict aliasing 的未定義行為（現行 AA 量化用的 `reinterpret_cast<uint64_t*>` 在 x86 上能動，但不在本次修改範圍）。
- **邊界**：迴圈條件 `p + 8 <= n` 保證絕不讀超過緩衝末端，不需要額外配置填充空間。
- **同色的定義**是「等於目前段的值」，因此全黑段、全白段（模型實心內部）都能一次跳 8 個。
- **`ctz64`**：以一個小的內聯函式包裝 `__builtin_ctzll`（GCC／Clang）與 `_BitScanForward64`（MSVC）。
- **位元組序**：`ctz >> 3` 的推論只在小端序成立。目前所有目標平台（x86_64、arm64）都是小端序；以編譯期判斷在大端序平台退回逐位元組路徑。
- **AA 灰邊密集時**：每次不相符都要多做一次載入、XOR 與 `ctz`，成本與原逐位元組迴圈接近，不會明顯變慢。這一點由滿版案例的 3% 門檻把關。

### D3：第 1 步預覽——改為列優先累加，零字組整段跳過

只修改整數倍快路徑 `preview_box_downscale_integer`；通用路徑不動（正式流程不會走到）。

現行迴圈順序是「目標列 → 目標欄 → 來源 n 列 → 來源 n 欄」，每處理一個目標像素就要在 n 條來源列之間跳動。新的順序改為「目標列 → 來源 n 列 → 沿著來源列連續走訪」：

```
for dy in 0..new_h:
    sums[0..new_w] = 0                       // unsigned，最大 n²×255 = 25,500
    for sy in dy*n .. dy*n+n:
        row = src + sy * w
        x = 0
        while x + 8 <= new_w * n:
            if load_u64(row + x) == 0: x += 8; continue
            for 這 8 個位元組中非零者: sums[x_i / n] += row[x_i]
            x += 8
        尾端逐位元組累加
    for dx in 0..new_w: dst[dy*new_w + dx] = sums[dx] / (n*n)
```

- **等價性**：每個目標像素累加的像素集合完全相同，整數加法可交換，最後的截斷除法也相同，因此輸出逐位元組不變。
- **走訪範圍**：只讀 `[0, new_w × n)` 欄與 `[0, new_h × n)` 列，與現行快路徑完全一致。快路徑本來就會忽略右側與底部不足一個區塊的像素（見 D7）。
- **8 位元組字組會跨越兩個目標區塊**（例如 n = 5）。這沒有關係：零字組對任何區塊的貢獻都是 0；非零字組則逐位元組歸屬到各自的 `sums[x / n]`。
- **暫存空間**：`sums` 是每次呼叫配置一次、長度 `new_w` 的陣列（16K 為 1,512 × 4 位元組），常駐 L1 快取。
- **2026-09-17 補記**：內容滿版時零字組幾乎跳不掉，每個位元組都要讀寫一次 `sums`，這是第 1 輪驗收滿版案例未通過的主因。n ∈ {4, 5, 8, 10} 的內層累加由 D13 取代，列優先的走訪順序保留。

### D4：第 2 步資料結構——每格 1 位元組的格子表

```
T        = 80                          // 格子邊長（像素），編譯期常數
tiles_x  = ceil(W / T)
tiles_y  = ceil(H / T)
m_tile   : std::vector<uint8_t>(tiles_x × tiles_y)   // 0 = 乾淨，1 = 寫過
m_row_any: std::vector<uint8_t>(tiles_y)             // 該格子列是否有任何一格寫過
```

**不變式 I1（乾淨即背景）**：`m_tile[ty × tiles_x + tx] == 0` ⇒ 該格內每個像素都等於背景值。
**不變式 I2（寫入必登記）**：任何像素被寫成非背景值之前，它所在的格子必須已經標為 1。允許多標（標了但沒寫），**不允許漏標**。

I1 + I2 讓緩衝**永遠是完整、正確的影像**：格子表只是「哪裡不必看」的提示。所以不讀格子表的舊使用端（PNG 編碼器、`AnycubicSLA`、`read_pixel`、GUI）完全不受影響。

**為什麼每格 1 位元組，而不是位元集合**：

| | `uint8_t` 陣列（採用） | 位元集合 |
| --- | --- | --- |
| 16K 大小 | 14,742 位元組 | 1,843 位元組 |
| 標記一段格子 | 一次 `memset` | 需處理跨字組的遮罩 |
| 讀取 | 直接索引，不需位移運算 | 需位移與遮罩 |

兩者相對於每條執行緒 94 MB 的緩衝都小到可以忽略；8 條執行緒合計約 118 KB。位元集合省下的空間沒有實際價值，卻讓標記與讀取都變複雜，因此採用位元組陣列。

**空間複雜度**：O(W × H / T²)。各機型的格子數：

| 幅面 | tiles_x × tiles_y | 格子表 | 不完整格子 |
| --- | --- | --- | --- |
| 15120 × 6230（16K） | 189 × 78 | 14,742 B | 底部一列高 70 px |
| 13320 × 5120（14K） | 167 × 64 | 10,688 B | 右側一欄寬 40 px |
| 7536 × 3240（8K） | 95 × 41 | 3,895 B | 右側寬 16 px、底部高 40 px |
| 5760 × 3600 | 72 × 45 | 3,240 B | 無 |
| 3840 × 2400／2160 | 48 × 30／27 | 1,440／1,296 B | 無 |

直式（portrait）方向時 W、H 會對調（[SL1.cpp:390-393](../../../third_party/prusaslicer_fork/src/libslic3r/Format/SL1.cpp#L390-L393)），不完整格子會換到另一個軸。因此 D7 的邊界處理必須兩軸對稱，不能假設只有底部不完整。

**為什麼 T = 80**：

- T 必須是 8 的倍數，格子邊界才會對齊 D2／D3 的 8 位元組字組。
- T 必須能被所有預覽分母 4、5、8、10 整除，也就是 40 的倍數。這樣每個預覽區塊一定完整落在同一格內（80 ÷ 10 = 8、÷ 5 = 16、÷ 4 = 20、÷ 8 = 10），不需要處理一個預覽區塊跨越兩格的情況。
- 在 40 的倍數中，40 的格子數是 80 的 4 倍（16K 為 58,968 格），每層的格子表處理量跟著變 4 倍；160 則太粗，一顆導板的邊緣就會把大片黑色格子標髒。80 是折衷的預設值，最佳值在實作後以量測決定（見 Open Questions）。

**替代方案（否決）：只記錄每列的最左、最右寫入位置。** 零件散佈在平台各處時，一列的左右界常常橫跨大半個幅面，吃不到中間的空白。前一個變更也已因同樣理由否決 bbox 級裁切。

### D5：第 2 步寫入記錄點——包一層像素格式轉接器

在 AGG 的像素格式（`agg::pixfmt_gray8`）外面包一層 `TileTrackingPixfmt<PixFmt>`，由它提供 `agg::renderer_base` 需要的方法：先依（已裁切的）座標標記格子，再轉呼叫真正的像素格式。

```
renderer_scanline_aa_solid          draw() 與 draw_binary() 共用 _draw()
        │
agg::renderer_base<TileTrackingPixfmt>    ← 在這裡完成裁切（clip box）
        │  blend_solid_hspan(x, y, len)   AA 邊緣
        │  blend_hline(x, y, len)         實心內部
        │  copy_hline(x, y, len)          clear()
        ▼
TileTrackingPixfmt  ── mark(y, x, x+len-1) ──► m_tile / m_row_any
        │
agg::pixfmt_gray8   ── 實際寫入 m_buf
```

`mark` 的計算：`ty = y / T`，`tx0 = x / T`，`tx1 = (x + len − 1) / T`，對 `m_tile[ty × tiles_x + tx0 .. tx1]` 做 `memset(1)`，並設 `m_row_any[ty] = 1`。

- **兩軌都自動涵蓋**：模型軌 `draw()` 與支撐軌 `draw_binary()` 都經由 `_draw()` → `render_scanlines` → `renderer_base`，所以兩者寫入時都會標記，不需要在呼叫端各自處理。
- **座標已經裁切**：`renderer_base` 在呼叫像素格式之前已經把跨出幅面的片段裁掉（[agg_renderer_base.h:275-297](../../../third_party/prusaslicer_fork/bundled_deps/agg/agg/agg_renderer_base.h#L275-L297)），轉接器拿到的座標一定在 `[0, W) × [0, H)` 內，不需要再檢查範圍。
- **鏡像與直式方向不影響**：路徑變換在光柵化之前就完成了，轉接器看到的是緩衝座標。
- **AA 灰邊一定被標到**：AA 邊緣的覆蓋像素本身就是 `blend_solid_hspan` 的一部分，不必另外向外擴張。
- **覆蓋率為 0 的像素也照標**：屬於多標，不違反 I2。

**只實作目前會被呼叫到的方法**（`width`、`height`、`blend_solid_hspan`、`blend_hline`、`copy_hline` 與型別別名）。C++ 樣板的成員函式只在被呼叫時才實體化，所以日後若有人在 raster 裡呼叫 `copy_bar` 之類的新寫入方法，會得到編譯錯誤，而不是一個沒有標記的靜默寫入。這與 `reset()` 設為純虛擬是同一個原則。

**替代方案（否決）：在 `_draw()` 裡自己跑掃描線迴圈，從掃描線的片段標記。** 程式較少，但片段還沒經過裁切，必須自己處理邊界；更重要的是，任何繞過 `_draw()` 的新寫入路徑都會靜默漏標。

### D6：第 2 步各消費端與執行緒重用時的重置

四個全幅面操作都改成「依格子表走訪」。它們共用同一個規則：從 `m_tile` 算出每個格子列裡「連續寫過的格子區段」，只處理這些區段，其餘直接視為背景值。**不另建快取**，每個消費端自己從格子表推導（16K 每層最多掃 14,742 位元組），避免快取失效造成的錯誤。

**① `reset()`**

```
for ty where m_row_any[ty]:
    y0 = ty × T; y1 = min(y0 + T, H)
    for 每個連續寫過的格子區段 [tx_a, tx_b]:
        x0 = tx_a × T; x1 = min((tx_b + 1) × T, W)
        for y in y0..y1: 以背景值填 m_buf[y × W + x0 .. y × W + x1)
memset(m_tile, 0); memset(m_row_any, 0)
重新套用建構時的 gamma（沿用現行行為）
```

- **直接寫入 `m_buf`，不經過轉接器**，否則清除本身會把所有格子重新標髒。
- **建構子**仍以 `clear(m_background)` 清除整張（會經過轉接器而全部標髒）；清完之後把格子表歸零。此時整張都是背景值，I1 成立。
- **執行緒重用的安全性**：格子表是 raster 實例的成員，而實例與執行緒綁定。無論工作竊取讓這條執行緒接下來處理哪一層，`reset()` 結束時 I1 對所有格子都成立，下一層與上一層是哪一層完全無關。「全部標髒」的格子表會退化成清除整張，也就是現行行為。

**② 後處理（AA 量化）**

後處理函式是 SL1 注入的 `std::function`，raster 無法得知它是否只看單一像素。因此新增一個明確的旗標：

```
struct RasterPostProcess {          // 包住現有的 RasterPostProcessor
    RasterPostProcessor fn;
    bool zero_preserving_pixel_local = false;   // 預設 false = 安全
};
```

- **旗標為 true** 表示：每個像素的輸出只取決於它自己，而且 `f(背景) == 背景`。SL1 的 AA 量化符合這兩點：`c == 0 || c == 255` 時直接返回，其餘像素各自獨立計算。SL1 只在 `blur == 0` 時設為 true。
- **為 true 時**，raster 只對寫過的區段逐列呼叫 `fn(m_buf + y × W + x0, x1 − x0, 1, 1)`。因為每個像素各自計算，把一整張拆成許多列段呼叫，結果與一次呼叫整張完全相同，SL1 的量化程式本體不必修改。乾淨格子全是 0，對它們呼叫量化也不會有任何變化，所以跳過是安全的。
- **為 false 時**（`blur > 0` 或任何未宣告的後處理），對整張緩衝呼叫一次，然後把所有格子標髒。blur 會把亮度擴散到鄰近的乾淨格子，全部標髒是最簡單且正確的做法。blur 路徑因此維持現行效能，符合 Non-Goals。
- `create_raster_grayscale_aa()` 增加一個預設為 `false` 的參數。沒有傳入的呼叫端（`AnycubicSLA`、GUI）行為不變。

**③ RLE 編碼**

在 D2 的迴圈外面加上格子走訪：

```
for y in 0..H:
    ty = y / T
    if !m_row_any[ty]:  以「W 個背景值」併入目前的同色段；continue
    for 該格子列依序每個區段 [x0, x1)（寫過與乾淨交替）:
        乾淨區段：以「x1 − x0 個背景值」併入目前的同色段
        寫過區段：對 src[y × W + x0 .. y × W + x1) 執行 D2 的字組比對
```

「併入 k 個值 v」的定義是：`v == cur` 時 `run += k`；否則先 `emit(cur, run)`，再設 `cur = v, run = k`。
**同色段會跨列延續**：`cur` 與 `run` 在換列時不重設，與原實作的列優先連續走訪一致。依 D1，最大同色段序列不變，因此輸出逐位元組相同。

**④ 預覽**

在整數倍快路徑中，條件 `T % n == 0` 成立時，每個目標像素 `(dx, dy)` 對應的來源區塊一定落在單一格子 `(dx × n / T, dy × n / T)` 內。對乾淨格子，目標值等於背景值（背景為 0 時 `dst` 已初始化為 0，直接不寫）；只有寫過的格子才執行 D3 的累加。若 `T % n != 0`（目前不會發生），退回 D3 的全幅面路徑。

**消費端介面**：不修改 `RasterEncoder` 的簽章。

- raster 新增唯讀存取 `const TileMap* written_tiles() const`（`RasterBase` 預設回傳 `nullptr`，SVG raster 不需實作）。
- `SLAArchiveWriter` 新增 `virtual SparseRasterEncoder get_sparse_encoder() const`，預設回傳空值。SL1 在 `SLA_LAYER_RLE` 開啟時回傳稀疏版 RLE 編碼器。
- `draw_layers()` 在稀疏編碼器存在且 `written_tiles()` 非空時使用稀疏版；否則照舊呼叫 `get_encoder()`。預覽同理。
- 其他格式（`AnycubicSLA`、`SL1_SVG`）不必修改。

### D7：不完整格子——所有區段右界與下界一律裁切

所有由格子換算像素範圍的地方，都使用同一組公式：

```
x0 = tx × T              x1 = min((tx + 1) × T, W)
y0 = ty × T              y1 = min((ty + 1) × T, H)
長度 = x1 − x0          （16K 最右一格 = 15120 − 15040 = 80）
高度 = y1 − y0          （16K 最下一列 = 6230 − 6160 = 70）
```

- **reset**：以裁切後的 `[x0, x1) × [y0, y1)` 填背景值，絕不寫到緩衝外。
- **RLE 的算術補正**：乾淨區段併入的背景值個數就是裁切後的 `x1 − x0`；整個格子列都乾淨時，每一列併入 `W` 個。16K 最後一個格子列有 70 列，每列併入 15,120 個背景值，所以總像素數恆為 `W × H`。
- **AA 量化**：傳給 `fn` 的長度就是裁切後的 `x1 − x0`。
- **預覽**：快路徑本來就只處理 `new_w × n` 欄與 `new_h × n` 列，目標索引額外以 `min(…, new_w)`、`min(…, new_h)` 裁切。例如 8K（n = 5）最右一格 x = 7520～7535 對應目標欄 `[1504, min(1520, 1507)) = [1504, 1507)`；來源第 7535 欄在現行快路徑中本來就不讀，稀疏版也不讀。
- **標記**：`tx1 = (x + len − 1) / T`，因為 `x + len − 1 ≤ W − 1`，結果一定小於 `tiles_x`，不需要額外裁切。

單元測試必須涵蓋：寬、高都不整除的幅面（例如 7536 × 3240 與其直式對調），以及只在最右欄、最下列、右下角單一像素寫入的情況。

### D8：保留原演算法作為對照組，並以環境變數切換

原本的逐位元組 RLE、區塊優先預覽、整張清除、整張後處理全部保留，作為參考實作：

- 環境變數 **`SLA_RASTER_FASTPATH=0`** 時，第 1 步與第 2 步都關閉，完全走參考實作。預設為開啟。沿用 `SLA_LAYER_RLE` 的既有慣例。
- 用途有三個：單元測試的差分對照組、同一支 binary 內的快速 A/B 比較、上線後出問題時不重新建置即可停用。
- **正式的「改動前」基準仍然必須由未修改的程式碼建置**。原因是參考實作與新程式碼在同一支 binary 裡，同一個重構錯誤可能同時影響兩者。

另外提供 **`SLA_RASTER_VERIFY=1`**：每層編碼後掃描整張緩衝，確認每個乾淨格子都是背景值（驗證 I1）；一旦違反就寫一行錯誤到 stderr，並讓切片以失敗結束。這個模式只在驗收與測試時開啟，不影響正式效能。

### D9：各階段計時

環境變數 **`SLA_RASTER_TIMING=1`** 時啟用。

| 計時點 | 位置 |
| --- | --- |
| `reset` | `draw_layers()` 的 `acquire()` 前後 |
| `draw_model`、`postprocess`、`draw_support` | `lvlfn` 的三段迴圈前後 |
| `encode_layer`、`encode_preview` | `draw_layers()` 的兩次 `encode()` 前後 |

- 每條執行緒以 `tbb::enumerable_thread_specific` 累加 `steady_clock` 的耗時，不在熱迴圈裡加鎖。每層只多 12 次時鐘讀取，相對於每層數十毫秒可以忽略。
- 光柵化結束時合併，寫**一行**到 stderr：`[raster-timing] {"layers":N,"threads":K,"wall_s":…,"thread_s":{"reset":…,…}}`。
- 各階段是「所有執行緒加總的秒數」，不是牆鐘時間，因此另外附上整段光柵化的牆鐘時間 `wall_s`。
- **寫到 stderr，不寫 stdout**：agent 的進度解析讀 stdout，而且 `test_slice_progress_string_contract.py` 鎖定了 stdout 的標記字串；stderr 只會被整份存成 `stderr.log`。

### D10：單元測試位置與內容

新增 `tests/sla_print/sla_raster_scan_tests.cpp`，並加入 `tests/sla_print/CMakeLists.txt`。所有測試都以參考實作（D8）當對照組，比對逐位元組結果：

- **編碼器**：全黑、全白、只有 (0,0)／(W−1,H−1) 單點、灰階雜訊、跨列延續的長同色段、段長恰為 15／16、4095／4096、1048575／1048576、緩衝長度不是 8 的倍數。
- **格子表**：不整除的幅面與其直式對調、只寫在不完整格子內、只寫在格子邊界兩側的相鄰像素、寫過後完全清空的層。
- **執行緒重用**：同一個 raster 連續處理「大面積層 → 空層 → 單點層 → 滿版層」，每層都與新建的 raster 結果相同。
- **後處理旗標**：`blur = 0` 時稀疏後處理與整張後處理相同；`blur = 1` 時所有格子被標髒，結果與參考實作相同。
- **亂數差分**：固定亂數種子產生多邊形集合，在多種幅面下比對稀疏版與參考實作。
- **預覽固定區塊核心（D13）**：每種區塊值、每一種總和、移動單點、週期不是 8 倍數的條紋、真實尺寸、防護頁越界、純量版本，細節見 D13。

### D11：基準資料與比對工具的 repo 配置

依循既有的 `scripts/check_baseline.py` 慣例：**病患相關的 STL 不入 git**（見 [scripts/README-surg-guide-regression.md](../../../scripts/README-surg-guide-regression.md)），repo 只放工具、清單與文字形式的結果。

```
scripts/raster_bench/                     ← 入 git
├── README.md                             使用方式、模型取得方式
├── manifest.json                         案例定義（見下）
├── make_fixtures.py                      產生排版 STL、合成平板、凍結支撐
├── run_bench.py                          跑引擎、蒐集時間與指紋
├── compare_fingerprints.py               比對兩組指紋
├── acceptance_report.py                  階段 3 門檻判定，產出 summary.json 與 report.md（D12）
└── tests/                                比對、排版與判定邏輯的 pytest

scripts/raster_bench/work/                ← gitignore（新增規則）
├── fixtures/<case>/model.stl             產生出來的排版模型
├── fixtures/<case>/support.stl           凍結的支撐網格
├── engines/final-10fcc6d96/              最終引擎副本與 build_info.json（D12）
├── prz/                                  端到端驗收下載的 PRZ（D12）
└── runs/<platform>-<build>-<case>-t<threads>-r<n>/
    ├── layers.sha256                     每層解壓後的指紋
    ├── preview.sha256                    每張預覽圖的指紋
    ├── timing.json                       牆鐘、各階段、[raster-timing]、峰值承諾記憶體與峰值 RSS
    └── meta.json                         平台、引擎身分（exe/dll SHA-256）、fork commit、實際執行緒數、參數雜湊

openspec/changes/optimize-raster-canvas-scan/evidence/   ← 入 git，只放文字
└── <platform>-<timestamp>/               驗收當次的 summary.json、report.md、指紋清單
```

**`manifest.json` 的內容**：

- 每個案例的機型、幅面、`config.ini` 參數、執行緒數與重複次數。
- 來源模型只記**檔名與 SHA-256**，不記絕對路徑。模型目錄由 `--models` 參數或環境變數 `RASTER_BENCH_MODELS` 指定；工具不硬寫路徑（避免重蹈 `check_baseline.py` 硬寫 macOS 路徑的問題）。
- 排版以每顆的 (x, y) 位移描述。16K 為 4 × 2、8K 為 3 × 1，格距固定。
- 凍結支撐的 SHA-256。
- 合成滿版平板（200 × 110 × 2 mm）由程式碼產生，不含病患資料，可以在任何機器重現。

**凍結支撐的流程**：`make_fixtures.py --freeze-support` 只在建立基準時執行一次，以「改動前」的引擎產生 `support.stl`，並把雜湊寫回 `manifest.json`（這次提交進 git）。之後 `run_bench.py` 發現支撐檔缺少或雜湊不符時直接拒絕執行，不會自動重新產生，以免在不知情的情況下換掉比較基礎。產出的支撐檔與來源模型放在同一個內部共享位置，不入 git。

**指紋的定義**：以 Python `zipfile` 讀出 `.sl1` 與預覽 zip 中每個項目**解壓後**的位元組計算 SHA-256，按項目名稱排序，每行 `<名稱>  <sha256>`。不對 zip 檔本身取雜湊，因為 zip 標頭含時間戳記，前一個變更已經因此誤判過一次。

**比對規則**（`compare_fingerprints.py`）：

- `meta.json` 的平台標籤不同時預設拒絕比對；加 `--allow-cross-platform` 只輸出差異報告，不作為驗收依據。
- 輸出層數是否相等、第一個不同的層、不同層的總數；任一層不同時結束碼為 1。

**驗收執行矩陣**：

1. 以改動前的引擎，用 `--threads 1` 與預設執行緒數各跑一次，**先確認基準本身在不同執行緒數下一致**，否則基準不可信。
2. 以改動後的引擎，對 `--threads 1`、`2`、`8` 各跑 3 次，每一次的指紋都必須等於第 1 步的基準。
3. 時間比較只取預設執行緒數下 3 次的中位數；比較基準、交替執行、重跑與判定方式見 D12。

**替代方案（否決）：把排版後的 STL 與支撐檔直接提交進 git。** 8 顆排版約 50 MB，repo 沒有使用 Git LFS，而且這些模型屬病患資料，違反既有慣例。

### D12：階段 3 的量測基準、判定工具與端到端比對

2026-09-17 在階段 3 開始前逐題定案。目的是讓門檻在量測之前就寫死，避免看到結果後才選規則。

**記憶體門檻改以承諾記憶體為主。** 原本的「峰值 RSS 不高於基準」在實測中等同擲硬幣：主基準基準 3 次為 1,444／1,439／1,441 MiB，T = 40 為 1,427／1,440／1,462 MiB，兩邊中位數只差 1.5 MiB，同一支引擎 3 次卻能差 35 MiB。峰值承諾記憶體（`PeakPagefileUsage`）則從約 8,033 MiB 降到約 2,209 MiB，3 次都很穩定，反映的才是實際改善。定案：

- 主門檻：主基準峰值承諾記憶體中位數不高於基準。
- 護欄：主基準峰值 RSS 中位數不高於基準的 103%，與滿版案例的時間容許值一致。

**基準取同一工作階段重跑的數字。** Golden 建立於 2026-09-14，最終驗收在數日之後，筆電溫度與系統狀態都可能不同。定案：

- 門檻比較的「基準」是最終驗收同一工作階段內，改動前引擎 `base-5bc83b08f` 重跑 3 次的中位數。適用於有門檻的三個案例（主基準、次基準、滿版）；極小案例只記錄。
- 改動前與最終引擎交替執行：`base r1 → final r1 → base r2 → final r2 → base r3 → final r3`。
- 指紋仍比對 9/14 凍結的 Golden。改動前引擎重跑的指紋若不等於 Golden，代表基準不具決定性，整輪作廢。

**成對重跑、上限 3 輪、輪次標籤。** `run_bench.py` 以 `平台-build-案例-[variant]-t緒數-r序號` 組目錄名，遇到已存在的目錄即拒絕；9/14 的 `windows-base-*-tdefault-r1..r3` 已佔用名稱。定案：

- 某案例任一組觸發重跑（全距同時大於中位數 5% 且大於 0.5 秒），兩組一起重新交替執行一整輪。
- 輪次標記 `a1`、`a2`、`a3`，只採最新一輪，舊輪標記 superseded 並保留。`a3` 仍觸發即標記 UNSTABLE，不給通過或不通過，排除環境干擾後該案例從 `a1` 重來。
- build 標籤帶輪次，例如 `p3-base-a1`、`p3-final-a1`，不修改 `run_bench.py` 的命名規則。

**多執行緒擴展性曲線只記錄。** 3.2 的 1／2／8 緒執行開著 `SLA_RASTER_VERIFY`，時間不能用來看擴展性；agent 不傳 `--threads`，線上一律使用硬體執行緒數（fork `src/libslic3r/Thread.cpp:242-243`）。定案：

- 主基準，改動前與最終引擎各以 `--threads 1`、`2`、`4`、`8` 跑 3 次，不設 `SLA_RASTER_VERIFY`、`SLA_RASTER_TIMING`。
- 記錄光柵化耗時、承諾記憶體、RSS 的中位數／最大／最小；不設門檻、不套重跑規則；指紋仍須全部等於 Golden。
- 8 緒端點直接採用效能門檻驗收中主基準最新一輪的預設執行緒數執行，前提是 `meta.json` 記錄的實際執行緒數為 8。

**引擎身分以二進位 SHA-256 定錨。** `work/engines/` 下階段 2 的 4 支引擎都沒有 `build_info.json`，`engine_build_id.txt` 皆為 `20260904T070913Z`，但二進位彼此不同；`step2-T40-570c7c5e0` 與 `step2-T40-570c7c5e8` 是兩份不同的 T = 40 建置；fork commit `10fcc6d96`（tree `4e2963b2a`）晚於 `…e8` 的建置，中間只改了 `RasterBase.hpp` 一段註解。定案：

- 最終引擎由使用者以 VS2022 從 `10fcc6d96` 建置（既有建置調整照舊留在工作區），副本放在 `work/engines/final-10fcc6d96/`，建置當下寫 `build_info.json`：commit、原始碼樹雜湊、建置調整 diff 的 SHA-256、建置組態、`slicer-engine.exe` 與 `slicer_core.dll` 的 SHA-256。
- `run_bench.py` 的 `meta.json` 改記兩個二進位的 SHA-256 作為引擎身分，並記錄實際生效的執行緒數（不再是 null）。
- 階段 3 的所有執行只准使用這一支；身分不符的執行作廢。

**跑判分離。** 過去各階段的 PowerShell 腳本自己算中位數與重跑條件、沒有測試，報表只列規則不判定，由人讀表。定案：

- 執行層：PowerShell 腳本只負責排程與交替呼叫 `run_bench.py`，不做任何門檻計算。
- 判定層：新增 `scripts/raster_bench/acceptance_report.py`，只讀 `work/` 內的紀錄，產出 `summary.json`（單一事實來源）與由它轉出的 `report.md`；任一 FAIL、UNSTABLE、指紋不符、身分不符或端到端比對失敗即以非零結束碼結束。
- 判定邏輯以 pytest 覆蓋規格中的每個 Scenario 數字。3.16、3.17 的審查以 `summary.json` 為依據。

**端到端 PRZ 採遮罩比對。** `agent/prz_encoder.py:522` 以 `datetime.now()` 寫入檔頭 File Time，兩次切片必然不同，原本「兩份 PRZ 逐位元組相同」一定失敗。欄位位置：Version 4 + Tag 8 + Software 32 + Software Version 24 = 偏移 68，長 24 位元組，與 `agent/prz_decoder.py:218` 一致；內容為 19 字元時間字串加 5 個 NUL。定案：

- 兩份長度相同；只遮罩 `[68, 92)`；兩份的遮罩區各自須是合法的 `%Y-%m-%d %H:%M:%S` 加 5 個 NUL；其餘位元組全部相同。
- 遮罩區外有差異即不通過，不得擴大遮罩。以關鍵字搜尋編碼器只找到這一個會變動的欄位，尚未以實際 PRZ 驗證；若實測發現其他差異，照規則判定不通過。
- 下載的 PRZ 由病患衍生模型切出，只存放於 `work/`，不入 git。

**macOS 延後。** 正式環境是 macOS Apple Silicon（`deploy/setup.sh` 以 `launchctl` 啟動、路徑 `/opt/homebrew`），但目前沒有可建置的 Apple Silicon Mac。定案：本變更只在 Windows x64 驗收；macOS 另開變更處理，通過前含本變更 fork 指標的版本不得部署到 macOS 正式環境（見 Open Questions）。

### D13：第 1 輪驗收未通過後的滿版修正——預覽固定區塊 SSE2 累加

2026-09-17 定案。第 1 輪效能門檻驗收（tasks.md 3.6、3.7）中滿版案例未通過，依 3.7「未達標時回到階段 2，不得自行放寬門檻」處理。

**第 1 輪結果。** 引擎 `final-10fcc6d96`，同一工作階段以 `p3-base-a1`／`p3-final-a1` 交替執行，18 次指紋全部等於 Golden，沒有觸發重跑，沒有 UNSTABLE：

| 案例 | 指標 | 基準中位數 | final 中位數 | 比例 | 門檻 | 結果 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 主基準 16K × 8 | 光柵化時間 | 22.82 s | 4.47 s | 19.6% | ≤ 50% | PASS |
| 主基準 16K × 8 | 峰值承諾記憶體 | 8,046 MiB | 2,216 MiB | 27.5% | ≤ 100% | PASS |
| 主基準 16K × 8 | 峰值 RSS | 1,457.5 MiB | 1,448.6 MiB | 99.4% | ≤ 103% | PASS |
| 次基準 8K × 3 | 光柵化時間 | 6.37 s | 1.30 s | 20.4% | ≤ 100% | PASS |
| 滿版 16K 平板 | 光柵化時間 | 2.132 s | 2.218 s | 104.0% | ≤ 103%（2.196 s） | **FAIL** |

- 滿版 3 次：基準 2.233／2.119／2.132 s，final 2.218／2.139／2.251 s。兩組全距都約 0.11 s，超標不是單次雜訊造成的。
- 極小案例（只記錄）與擴展性曲線（3.26）在第 1 輪沒有執行。
- 與 Open Questions 中 2.23 的「T = 40 滿版 92.4%」不一致。那次是與 9/14 Golden 時期的基準跨工作階段比較，引擎也是 `step2-T40-570c7c5e0`／`…e8`，不是 `10fcc6d96`。判定以同一工作階段交替執行的 104.0% 為準；兩者差異的原因沒有另行查證。

**根因。** 3.5 的 `SLA_RASTER_TIMING=1` 診斷（step1 → final，8 執行緒，3 次中位數，單位 `thread_s`）：

| reset | draw_model | postprocess | draw_support | encode_layer | encode_preview | 合計 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 3.47 → 2.67 | 2.84 → 2.92 | 1.14 → 1.00 | 0 → 0 | 1.15 → 1.56 | **7.47 → 9.02** | 16.06 → 17.16 |

- step1 單執行緒（`t1-r1`）光柵化 6.35 s，其中 `encode_preview` 占 3.73 s（59%）。預覽是滿版案例最大的一塊成本。
- 格子表本身不搬移、也不還原任何像素；所有消費端直接讀原本連續的 `m_buf`。
- 滿版平板點亮 87.8% 的畫布，已寫入格子的內部幾乎全是 255，D3 的零字組跳過幾乎不生效。於是每個位元組都執行一次 `sums[bx] += row[i]`：讀出、相加、寫回，下一個位元組又讀同一格，迴圈被「寫入後立刻讀回」的等待綁住。16K 案例的預覽倍數是 n = 10，8K 是 n = 5。
- final 比 step1 更慢的具體原因沒有以反組譯或單執行緒計時證實。推測是 `preview_block_row` 以參照接收 `sums`，而 MSVC 不做型別別名分析，可能在每次寫入後重讀陣列位址。本決定不依賴這個推測是否成立。
- 稀疏 RLE 的 +0.41 thread_s 可能來自每條像素列重掃一次格子列，以及同色段狀態被 lambda 以參照捕捉；同樣未證實。

**定案：固定區塊倍數的 SSE2 累加與常數除法。**

- 只改 `preview_block_row`。`preview_box_downscale_integer`（沒有格子表時的快路徑）改為逐列呼叫它，兩者共用同一個核心，既有的「快路徑對參考實作」測試因此也覆蓋新核心。
- `n ∈ {4, 5, 8, 10}` 以 `switch` 分派到 `N` 為編譯期常數的樣板；其他 n 維持現行逐位元組迴圈，不做任何修改。
- 區塊加總：以 `memcpy` 把區塊內剛好 `len`（1～8）個位元組載入先清為 0 的 64 位元整數。x64 以 `_mm_cvtsi64_si128` 加 `_mm_sad_epu8` 求和；N = 10 分成 8 與 2 位元組兩次。非 x64（macOS arm64）使用結果相同的純量版本。
- 保留 D3 的列優先走訪。每個區塊每條來源列只寫一次 `sums`，累加陣列以區域指標取用。
- 除法：`divide_fixed<N>` 以 `constexpr` 的 `N × N` 為除數，由編譯器換成移位或乘法；不手寫魔術常數。
- D8 的參考實作一字不改；`SLA_RASTER_FASTPATH=0` 的行為不變。
- 取捨：已寫入格子內的全零 8 位元組跳過不再保留。主基準與次基準的預覽也走這條路徑，是否退步由重跑的門檻驗收確認。

```
preview_block_row(dy, dx0, dx1, n):
    s = sums.data(); out = dst + dy * new_w; block_row = src + dy * n * w
    s[dx0..dx1) = 0
    switch n:
        case 4, 5, 8, 10:
            for sy in 0..N:                        // 列優先
                row = block_row + sy * w
                for dx in dx0..dx1:
                    p = row + dx * N               // 區塊 [p, p + N)
                    acc = 0
                    每次取 min(8, 剩餘) 個位元組 → memcpy 進清零的 uint64 → SAD
                    s[dx] += acc                   // 每區塊每列寫一次
            for dx in dx0..dx1: out[dx] = s[dx] / (N * N)   // N 為常數
        default:
            現行逐位元組迴圈
```

**越界保證（由結構保證，不靠執行期檢查）。**

- 每次載入的長度就是區塊剩下的位元組數（最多 8），讀取一定落在 `[dx × N, dx × N + N)` 內。
- `dx < dx1 ≤ new_w`，整數倍快路徑的前提 `new_w × N ≤ w` 保證區塊在來源列內；稀疏路徑另以 `min(cols.end / n, new_w)` 裁切。
- 來源列最大為 `new_h × N − 1`，前提 `new_h × N ≤ h` 保證在緩衝區內。
- 不使用任何 16 位元組載入（`_mm_loadu_si128`），也不使用要求對齊的 `_mm_load_si128`；`memcpy` 沒有對齊要求。

**等價性。** 每個目標像素累加的像素集合不變；無號整數加法可交換，且最大值 100 × 255 = 25,500 不會溢位；常數除法依語言規則等於一般的截斷除法。因此預覽圖逐位元組不變，不影響 Golden 指紋。

**單元測試（補充 D10）。** 一律以 `preview_box_downscale_integer_reference` 為對照組，逐位元組比對：

- 每種區塊值：n = 4、5、8、10，畫布為 n 的整數倍，每個區塊填同一值，0～255 全部出現。
- 每一種總和：每個 n 以單一 n × n 區塊窮舉總和 0～255 × n²（n = 10 共 25,501 種）。
- 移動單點：3n × 2n 畫布，背景 100，每個區塊的首像素為 99（每個區塊平均為 99），每次只對一個目標位置加 1，走遍所有位置；只有該位置所在的區塊應變為 100。不採用「全黑背景放值 1」，因為值 1 經除法截斷為 0，算錯區塊或漏算都看不出來。
- 週期 3、7、11 的直條紋與斜條紋，覆蓋 8 位元組載入與區塊邊界錯開的位置。
- 真實尺寸：15120 × 6230、n = 10 的反鋸齒滿版平板，以及 7536 × 3240、n = 5；稀疏版對非稀疏版（乾淨區域填毒值），非稀疏版對參考實作。
- 防護頁：Windows 以 `VirtualAlloc`／`VirtualProtect`、POSIX 以 `mmap`／`mprotect`，讓緩衝區尾端（另一組為開頭）緊貼不可讀的頁面；畫布為 n 的整數倍、內容全白、格子全部標記。任何越界讀取都會讓測試程序崩潰。
- 起點對齊：獨立測試，把同一份內容複製到 16 個連續偏移，涵蓋起點對 16 的餘數 0～15。不併入防護頁測試，因為防護頁測試的畫布必須是 n 的整數倍，n = 4、8 時緩衝區大小必為 16 的倍數，起點餘數固定為 0。
- 純量版本：以僅供測試使用的入口強制走純量核心，在 Windows 上執行前三項。
- 既有的 `[raster-scan]` 測試全部保留。

**未採納。**

- 只改寫法、不用 SIMD：審查最容易，但效益取決於編譯器是否自動向量化，餘裕不確定。
- 同時改寫稀疏 RLE：`encode_layer` 只多 0.41 thread_s，保留為本修正仍不足時的下一步。
- 16 位元組多區塊載入：只對 n = 4、8 有利，目前沒有案例使用。

**引擎身分與資產過渡。**

- fork 在 `10fcc6d96` 之上新增一個獨立 commit（`22f2e310a`，完整雜湊 `22f2e310ae6cdbeb3f9f0419fcc8e9d03b4e3fe6`），不改寫 `10fcc6d96`。
- 新的最終引擎必須由 `22f2e310a` 建置（既有建置調整照舊留在工作區），複製到 `<work>/engines/final-22f2e310a/`，並在建置完成當下寫 `build_info.json`。提交前的測試建置不得用來定錨。
- `<work>/engines/final-10fcc6d96/` 保留不動，作為封存紀錄的身分對照。D12「階段 3 的所有執行只准使用這一支」自此指 `final-22f2e310a`。
- `run_bench.py` 的目錄名只含標籤、不含引擎，第 1 輪的紀錄會與重跑撞名。新引擎定錨後、第一次重跑前，把第 1 輪的 `p3-*` 執行、`acceptance/` 的產出與相關 log 移到 `<work>/superseded/<UTC 時間>-phase3-iter1-final-10fcc6d96/`，附 `archive.json`，只搬不刪。改動前引擎的 `p3-base-a1` 一併封存，因為門檻基準必須在同一工作階段重跑。Golden 與 step1、step2 的紀錄不動。
- 重跑沿用 `p3-base-aN`／`p3-final-aN` 與原本的診斷標籤。排程腳本與 `acceptance_report.py` 以引擎名稱參數指向 `final-22f2e310a`，不修改工具程式碼。
- tasks.md 的 3.1～3.6 取消勾選，依原順序以 `final-22f2e310a` 重做；第 1 輪的結果保留在 tasks.md 的紀錄中。evidence 另外存放第 1 輪的 `summary.json`（改名為 `iteration1-summary.json`）與 `final-10fcc6d96` 的 `build_info.json`，讓「未通過 → 修正 → 重新驗收」的過程可以追溯。

## Risks / Trade-offs

- **[漏標寫入區域 → 靜默的錯誤像素，而且可能只在特定執行緒排程下出現]** → 標記放在 AGG 的最底層寫入點，未實作的寫入方法會編譯失敗（D5）；`SLA_RASTER_VERIFY=1` 在驗收時逐層檢查 I1（D8）；以多種執行緒數與重複執行比對指紋（D11）；單元測試以參考實作做差分對照（D10）。
- **[後處理旗標被誤用：非逐像素的後處理被標成 true]** → 旗標名稱直接寫出兩個條件（逐像素、背景不變），預設為 false；SL1 只在 `blur == 0` 時設為 true；`blur = 1` 的指紋比對納入測試。
- **[滿版時標記與格子走訪的額外成本]** → 由滿版平板案例的 3% 門檻把關；若超過，先評估較大的 T，或在轉接器記住上一次標記的格子範圍以省略重複的 `memset`。第 1 輪驗收實際超標（104.0%），但診斷顯示增量主要在預覽累加，不在格子表或轉接器；以 D13 的固定區塊 SSE2 累加處理，稀疏 RLE 的改寫保留為下一步。
- **[AA 灰邊密集的層讓第 1 步的字組比對常常失敗]** → 最壞情況與原逐位元組迴圈相近；以標準案例的逐階段計時確認沒有退步。
- **[未定義行為與位元組序]** → 一律以 `memcpy` 載入字組；`ctz` 推論只在小端序啟用，編譯期判斷。
- **[基準漂移：支撐檔或模型被換掉]** → 雜湊記錄在 `manifest.json`，不符時拒絕執行。`model.stl` 同時綁定排版定義的雜湊，凍結支撐綁定它所屬的 `model.stl`，改了排版卻沿用舊模型或舊支撐都會被擋下。
- **[筆電量測雜訊（i7-11370H 會降頻）]** → 插電、每輪之間冷卻、取 3 次中位數並記錄最大與最小值；改動前與最終引擎在同一工作階段交替執行；若任一組最大與最小相差同時超過中位數的 5% 且絕對差值大於 0.5 秒，兩組一起重跑，最多 3 輪，仍不收斂即標記 UNSTABLE（D12）。
- **[峰值 RSS 抖動蓋過真實差異]** → 記憶體主門檻改用峰值承諾記憶體，RSS 只保留 103% 護欄（D12）。
- **[改動前後的建置環境不同]** → 兩支引擎以同一套編譯設定、在同一台機器上建置；`meta.json` 以 `slicer-engine.exe` 與 `slicer_core.dll` 的 SHA-256 記錄引擎身分，並記錄 fork commit（D12）。
- **[病患資料外流]** → STL、支撐檔與由它們切出的 `.sl1`、`.prz` 一律只放在 `work/`、不入 git；evidence 只放雜湊與時間等文字。
- **[正式環境是 macOS arm64，但只在 Windows x64 驗收]** → Mac 專屬路徑（`ctz64` 的 `__builtin_ctzll` 分支、`is_little_endian` 的 boost 判斷、clang 編譯的轉接器防呆、單元測試）尚未執行過；`is_little_endian` 判斷失敗時快速路徑會靜默關閉，指紋比對抓不到。由後續 macOS 變更驗收，通過前不得部署到 macOS 正式環境（D12）。
- **[新增的虛擬函式與參數被其他格式誤用]** → 全部有安全的預設值（`nullptr`、空編碼器、`false`），不實作就等同現行行為。

## Migration Plan

| 階段 | 內容 | 驗收 | 回滾 |
| --- | --- | --- | --- |
| 0 | 建立 `scripts/raster_bench/`、凍結支撐、以未修改的引擎產生基準指紋與時間 | 基準在 `--threads 1` 與預設執行緒數下一致 | 刪除工具目錄即可，不影響產品 |
| 1 | fork：D9 計時 + D2 RLE 字組比對 + D3 預覽列優先累加 + D8 參考實作與切換 | D10 單元測試；三種執行緒數 × 3 次指紋全部相符 | fork 內 revert，或設 `SLA_RASTER_FASTPATH=0` |
| 2 | fork：D4～D7 格子表、轉接器、稀疏消費端、後處理旗標 | 同上，另加 `SLA_RASTER_VERIFY=1` 全程無違反；主基準光柵化耗時 −50%；滿版 ≤ +3%；主基準峰值承諾記憶體不增加、峰值 RSS ≤ +3% | fork 內 revert，或設 `SLA_RASTER_FASTPATH=0` |
| 3 | 父 repo 更新 submodule 指標、重新打包引擎、8K 次基準驗證、evidence 歸檔 | 端到端切片產出的 PRZ 除檔頭產生時間 `[68, 92)` 外與改動前逐位元組相同（D12）；`acceptance_report.py` 結束碼為 0 | 退回 submodule 指標 |

- 階段 1 與 2 於 fork 內統一為單一 commit `10fcc6d96`（兩階段的改動交錯在同一批檔案，無法乾淨拆開，見 tasks.md 1.18）。第 1 輪驗收後的滿版修正（D13）是其上的一個獨立 commit，不改寫 `10fcc6d96`。父 repo 的指標更新另外一個 commit，指向修正後的 commit。
- agent 與 DS-Online 都不需要修改。部署順序只有一個相依：macOS 變更驗收通過前，含本變更 fork 指標的版本不得部署到 macOS 正式環境（D12）。

## Open Questions

- **格子邊長的最佳值（已於 2.23 實測定案，2026-09-16）：T = 40**。

  量測條件：
  - 三版引擎出自同一個 `<vs_build>` 與 `Release | x64`，只有 `TileMap::T` 不同。
  - Windows 驗收機，預設執行緒數，每個案例 3 次取中位數，每次執行前閒置 60 秒。
  - **不設任何 `SLA_RASTER_*`**。`SLA_RASTER_VERIFY=1` 會逐層掃描整張畫布，成本會蓋過要量的數字（同一版引擎在開啟後主基準為 9.52 s，關閉後為 4.77 s）。正確性由 2.19 負責，這裡只量時間。
  - 每一次執行的層檔與預覽圖指紋都與 Golden 比對，全部相符：改動 T 不會改變輸出的任何一個位元組。

  | T | 主基準 16K × 8 中位數（最小～最大） | vs 基準 | 滿版 16K 平板 中位數（最小～最大） | vs 基準 | 主基準峰值 RSS |
  | ---: | --- | ---: | --- | ---: | ---: |
  | 基準 | 22.58 s（22.39～23.28） | 100.0% | 2.18 s（2.08～2.20） | 100.0% | 1441 MB |
  | **40** | **4.40 s（4.25～4.53）** | **19.5%** | **2.02 s（1.96～2.09）** | **92.4%** | **1440 MB** |
  | 80 | 4.77 s（4.67～4.89） | 21.1% | 2.08 s（2.07～2.14） | 95.3% | 1476 MB |
  | 160 | 5.65 s（5.58～5.68） | 25.0% | 2.11 s（2.01～2.21） | 96.7% | 1440 MB |

  定案理由：

  - **主基準單調偏好小格子**：19.5% → 21.1% → 25.0%，T 每加倍就慢一截。牙科導板是細長結構，格子越大，被一小段邊緣標髒卻幾乎全黑的區域就越多。T = 40 比 T = 80 快 7.8%，比 T = 160 快 22.1%。
  - **三組的離散度都不需重跑**：差距分別為 0.28 s、0.22 s、0.10 s，絕對值都遠小於 0.5 s 的門檻。三者的差異大於各自的雜訊，排序是可信的。
  - **滿版案例沒有反向代價**：T = 40 的格子表是 T = 160 的 16 倍大（16K 為 58,968 格對 3,744 格），但即使在每個格子都髒、完全跳不掉任何東西的滿版案例，T = 40 仍是三者中最快的 92.4%。格子表本身的走訪成本小到量不出來。這推翻了 D4 當初「40 的格子表處理量變 4 倍」的顧慮。
  - **峰值記憶體合規**：主基準門檻為「不高於基準」。T = 40 為 1440 MB，略低於基準的 1441 MB。T = 80 的 1476 MB（+2.4%）曾一度看似超標，但 T = 160 又回到 1440 MB，而 T = 40 的格子表比 T = 80 大 4 倍卻用得更少——可見那 35 MB 與格子表大小無關，是峰值工作集的量測抖動。選定 T = 40 之後這個疑慮不存在。（後續：這個抖動讓「RSS 不高於基準」的判定不可靠，階段 3 的記憶體主門檻已改為峰值承諾記憶體，RSS 保留 103% 護欄，見 D12。）
  - **T = 40 是格式允許的下限**：`T % 40 == 0` 是編譯期斷言，來自「預覽的每個 n × n 區塊（n = 4, 5, 8, 10）必須完整落在單一格子內」。再往下就會讓區塊跨格，稀疏預覽的前提失效。所以 40 不只是三者中最好，也是這個約束下能取到的最小值；趨勢雖然指向更小更好，但沒有再往下的空間。
  - 16K 幅面下 T = 40 的格子表為 378 × 156 = 58,968 格，加上 156 個列摘要，共 59,124 位元組，仍遠小於 L2 快取。

  後續影響：單元測試中依 T 推導的期望值（後處理呼叫次數、跨格拼接的同色段長度、預覽目標欄映射、格子座標）已全數改以 T = 40 重算。上方階段 0 表格中的「T=80 髒格子」欄位是當時的量測紀錄，不隨本決定改寫。
- **模型與凍結支撐的內部共享位置**：比照 `scripts/README-surg-guide-regression.md`，目前也還沒有確定的路徑，需由團隊指定後寫進 `scripts/raster_bench/README.md`。在指定之前，驗收可以在本機模型目錄進行，但不寫進任何入庫檔案。
- **macOS 是否需要跑完整驗收矩陣（已定案，2026-09-17）：本變更不驗 macOS，延後至獨立變更。**
  - 目前沒有可建置的 Apple Silicon Mac，本變更的驗收只在 Windows x64 驗收機上執行與認定，tasks.md 3.10 標記為延後。
  - 正式環境是 macOS Apple Silicon，所以延後的不只是「跑一次指紋」：Mac 上的建置相容性、`[raster-scan]` 單元測試、`is_little_endian` 判斷、`ctz64` 的 `__builtin_ctzll` 分支、macOS 專屬 Golden 與指紋驗證，都由後續 macOS 變更負責。`run_bench.py` 在 Mac 上不量記憶體，引擎身分的檔名也須改用 Mac 的實際檔名。
  - 在 macOS 變更驗收通過前，含本變更 fork 指標的版本不得部署到 macOS 正式環境。
- **各案例的層數、覆蓋率與基準耗時**（已於階段 0 實測回填，2026-09-14）：

  量測條件：
  - 引擎 `base-5bc83b08f`（fork tree `561f61f02`），Windows 驗收機，預設 8 執行緒。
  - 關閉 VS2022 與 Chrome，開跑前 CPU 負載 3%～13%。
  - 每次執行前閒置 60 秒。
  - 覆蓋率以 Golden（`tdefault-r1`）的 RLE 層檔逐層解碼計算。「點亮」指像素值大於 0。「髒格子」以 T = 80 計算，指含有任何點亮像素的格子。

  | 案例 | 層數 | 點亮比例 平均／最大 | T=80 髒格子 平均／最大 | 光柵化中位數（最小～最大，差距） | 每層 | 總時間中位數 | 峰值 RSS 中位數（最小～最大） |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | 主基準 16K × 8 | 592 | 4.66% ／ 13.82% | 10.20% ／ 24.10% | 22.58 s（22.39～23.28，3.92%） | 38.1 ms | 32.25 s | 1441 MB（1439～1444） |
  | 次基準 8K × 3 | 592 | 3.76% ／ 11.10% | 9.80% ／ 21.46% | 6.24 s（6.22～6.29，1.21%） | 10.5 ms | 9.84 s | 567 MB（565～608） |
  | 極小 16K 牙冠 | 186 | 0.08% ／ 0.13% | 0.17% ／ 0.26% | 7.33 s（7.14～7.54，**5.48%**） | 39.4 ms | 7.68 s | 800 MB（798～800） |
  | 滿版 16K 平板 | 40 | 87.81% ／ 87.81% | 89.85% ／ 89.85% | 2.18 s（2.08～2.20，**5.60%**） | 54.6 ms | 2.46 s | 783 MB（783～784） |

  - 這些數據支持「成本由畫布大小決定，與內容多寡無關」的判斷：極小案例只點亮 0.08%，每層耗時卻與主基準相同（約 39 ms）。在 16K 下，極小案例只有 0.17% 的格子是髒的，是 dirty-tile 效益的上限參考。主基準平均 10.2%、最多 24.1%。
  - 所有層都不是空層。
  - **離散度判定**：重跑規則為「相差同時超過中位數的 5% 且絕對差值大於 0.5 秒」。極小案例（5.48%，差 0.40 s）與滿版案例（5.60%，差 0.12 s）相對差距略高於 5%，但絕對差值都未大於 0.5 秒，不需重跑。主基準（3.92%，差 0.89 s）與次基準（1.21%，差 0.08 s）也都不需重跑，四組全部合格。這批數據前面曾有兩批量測受背景負載影響，已作廢。
  - 滿版案例的門檻是「不高於基準的 103%」，而它自身 3 次的離散約 5.6%，比 3% 的容許值還大。僅用 3 次中位數可能無法可靠判定，需另行決定。
  - 次基準的峰值 RSS 3 次差距為 7.6%（其中一次為 608 MB）。規格的 RSS 門檻只適用於主基準，主基準的 RSS 差距為 0.4%。
  - 改動後的量測應沿用相同條件：安靜環境，每次執行前閒置 60 秒。
- **階段 1 對照組（1.16，採用 1.12 的量測，2026-09-15，未重跑）**：階段 2 的耗時與各階段分佈以此為比較基礎。

  量測條件：
  - 引擎 `step1-570c7c5e2`（原始碼樹 `570c7c5e2`），預設 8 執行緒，`SLA_RASTER_TIMING=1`。
  - 每次執行前閒置 60 秒；開跑前的 CPU 負載未記錄。
  - 各次層檔與預覽圖指紋皆等於 Golden。

  主基準 16K × 8（`tdefault-r1`～`r3`）：

  | 項目 | r1 | r2 | r3 | 中位數 |
  | --- | --- | --- | --- | --- |
  | 光柵化時間 | 16.88 s | 16.67 s | 16.59 s | **16.67 s**（差距 1.70%） |
  | 總時間 | 26.63 s | 26.24 s | 26.14 s | 26.24 s |
  | 峰值 RSS | 1469 MB | 1435 MB | 1492 MB | 1469 MB |

  各階段 `thread_s`（所有執行緒加總的秒數，3 次中位數）：

  | reset | draw_model | postprocess | draw_support | encode_layer | encode_preview | 合計 |
  | --- | --- | --- | --- | --- | --- | --- |
  | 43.65 s（33%） | 7.38 s（6%） | 21.32 s（16%） | 3.32 s（3%） | 23.95 s（18%） | 31.54 s（24%） | 131.16 s |

  - 光柵化中位數 16.67 s，不高於 Golden 的 22.58 s（快 26.2%），符合 1.16 的條件。峰值 RSS 中位數 1469 MB，比 Golden 的 1441 MB 高 1.9%，3 次差距 3.9%。
  - 單執行緒（`t1-r1`）光柵化 38.94 s，各階段合計 38.93 s，其中 reset 佔 36%。8 執行緒的合計是單執行緒的 3.4 倍，表示多執行緒時每層成本上升，推測與記憶體頻寬競爭有關，尚未另行驗證。
  - 階段 2 的主要目標是 reset（33%）與 postprocess（16%），兩者目前都對整張畫布操作。
  - 其他案例的光柵化中位數（step1，3 次）：次基準 4.62 s（Golden 6.24 s）、極小 5.45 s（7.33 s）、滿版 2.22 s（2.18 s）。滿版差距 +1.8%，落在雙方離散範圍內。
- **階段 3 最終確認（已於 3.12 回填，2026-09-18）**：第 2 輪驗收，最終引擎 `final-22f2e310a`；數據見 `evidence/windows-20260918T015413Z/`。
  - **層數與點亮比例**：所有驗收執行的層數與階段 0 相同（主基準 592、次基準 592、極小 186、滿版 40）。36 次 `SLA_RASTER_VERIFY=1` 執行與 39 次驗收執行的層檔與預覽圖指紋全部等於 Golden。輸出逐位元組不變，所以上方以 Golden 計算的點亮比例與髒格子比例就是最終值，不需重算。
  - **T = 40 的最終確認**：與改動前引擎在同一工作階段交替量測（D12），主基準光柵化 22.61 s → 4.14 s（18.3%），與 2.23 選定 T 時的 19.5% 一致。峰值承諾記憶體 8044.2 MiB → 2228.4 MiB（27.7%），峰值 RSS 100.0%（護欄 103%）。次基準 6.22 s → 1.35 s（21.7%）。定案不變。
  - **滿版案例與 D13 的效果**：第 1 輪（`final-10fcc6d96`）滿版為 104.0%（2.132 s → 2.218 s），超過 103%。D13 之後，同樣的計時診斷（3 次中位數，`thread_s`）如下：
    - `encode_preview`：step1 7.47、第 1 輪 9.02、第 2 輪 3.47，低於 step1 的一半。
    - 各階段合計：step1 16.06、第 1 輪 17.16、第 2 輪 12.08。
    - 正式量測滿版 2.206 s → 1.760 s（79.8%）。
    - `encode_layer` 仍比 step1 多 0.45（1.15 → 1.59），與第 1 輪相同，是滿版案例中格子表與轉接器剩下的成本；D13 沒有處理它，但其節省已完全抵銷。
  - **階段 0「滿版只用 3 次中位數可能無法可靠判定」**：由 D12 的同工作階段交替量測與重跑規則處理。第 2 輪滿版的 3 次差距為 base 0.22 s、final 0.08 s，都沒有觸發重跑，79.8% 與 103% 的距離也遠大於離散。此項結案。
  - **端到端 PRZ**：兩份皆 103,667,342 位元組，只在檔頭時間欄位內的偏移 85、86 不同，遮罩區外 0 位元組差異。D12 中「編碼器只有 File Time 會變動、尚未以實際 PRZ 驗證」的假設已由實測證實。
  - **擴展性曲線（只記錄）**：主基準 final 在 1、2、4、8 緒為 13.43、7.44、4.87、4.14 s，是 base 的 15.6%、17.7%、18.4%、18.3%。
  - 仍未結案：模型與凍結支撐的內部共享位置（見上方條目）；macOS 驗收（延後至獨立變更）。
